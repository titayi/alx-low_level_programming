#include "main.h"

/**
 * _strlen - function that returns the length of a string
 * @s: the character
 * Return: value of m
 */
int _strlen(char *s)
{
	int m;

	for (m = 0; s[m] != '\0'; m++)

	return (m);
}
